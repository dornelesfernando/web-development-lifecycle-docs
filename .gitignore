# Node modules (se você adicionar algum código Node/JS no futuro)
node_modules/

# Variáveis de ambiente (se você usar .env no futuro)
.env
.env.local

# Artefatos de build (se você compilar algo no futuro)
build/
dist/

# Logs de debug do npm/yarn
npm-debug.log*
yarn-debug.log*

# Arquivos gerados pelo sistema operacional (já no global, mas bom ter aqui tbm por segurança)
.DS_Store
Thumbs.db

# Arquivos de cache do editor/outros
*.bak
*.tmp

# Arquivos de auxílio
backend/src/config/index.ts